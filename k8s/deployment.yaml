apiVersion: apps/v1
kind: Deployment
metadata:
  name: mto-backend
  labels:
    app: mto-treasury
spec:
  replicas: 3
  selector:
    matchLabels:
      app: mto-treasury
  template:
    metadata:
      labels:
        app: mto-treasury
    spec:
      containers:
      - name: mto-backend
        # Pin to a specific digest in production — the CI pipeline substitutes
        # this with the SHA from the build step:
        #   image: registry/mto-backend@sha256:<digest>
        # Using :latest is intentionally avoided — it silently pulls a
        # different image on node restarts and makes rollbacks unreliable.
        image: mto-treasury:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
          name: http

        # Non-secret configuration from ConfigMap
        envFrom:
        - configMapRef:
            name: mto-config

        # Secrets injected as environment variables from a K8s Secret.
        # Create the secret before deploying:
        #   kubectl create secret generic mto-secrets \
        #     --from-literal=MTO_JWT_SECRET=<value> \
        #     --from-literal=MTO_DB_PASSWORD=<value> \
        #     --from-literal=GRAFANA_ADMIN_PASSWORD=<value> \
        #     --from-literal=REDIS_PASSWORD=<value>
        envFrom:
        - configMapRef:
            name: mto-config
        - secretRef:
            name: mto-secrets

        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "200m"
            memory: "256Mi"

        # Liveness probe — restarts the container if the app is deadlocked
        # or has crashed. Uses /healthz which checks DB connectivity, cache,
        # storage, and vault — not just "is the process running".
        # The root endpoint (/) only returns {"message": "...online"} and
        # does not verify any dependencies are healthy.
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8000
          initialDelaySeconds: 20
          periodSeconds: 30
          failureThreshold: 3
          timeoutSeconds: 5

        # Readiness probe — gates traffic. K8s will not send requests to this
        # pod until /healthz returns 200. This prevents traffic from reaching
        # a pod that is still running migrations, warming up the DB pool, or
        # has lost its DB connection mid-deployment.
        # Uses a shorter period than liveness so traffic is restored quickly
        # after a transient DB blip.
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 10
          failureThreshold: 3
          timeoutSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: mto-service
spec:
  selector:
    app: mto-treasury
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      name: http
  type: LoadBalancer
